Place overrides in this folder.
There are two different supported algorithms: replace and merge
The default algorithm is "merge". In order to specify "replace", be sure to add "UifwMergeAlgorithm" : "replace"' as a property in the FDN.json itself.